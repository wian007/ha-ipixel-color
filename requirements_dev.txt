# ──────────────────────────────────────────────────────────────────────────────
# Development dependencies for the iPIXEL Color HA integration
#
# Install into a virtual environment:
#   python -m venv .venv
#   source .venv/bin/activate      # Windows: .venv\Scripts\activate
#   pip install -r requirements_dev.txt
#
# Or simply run:  make setup
# ──────────────────────────────────────────────────────────────────────────────

# ── Home Assistant & testing framework ──────────────────────────────────────
# pytest-homeassistant-custom-component provides a real (but sandboxed) HA
# core instance plus fixtures like `hass`, `config_entry`, and Bluetooth mocks.
pytest-homeassistant-custom-component>=0.13.0

# ── Integration runtime requirements (mirrored from manifest.json) ───────────
bleak>=0.20.0
bleak-retry-connector>=3.5.0
pillow>=10.0.0
pypixelcolor>=0.4.0

# ── Test utilities ───────────────────────────────────────────────────────────
pytest>=7.4
pytest-asyncio>=0.23
pytest-cov>=4.1        # Coverage reporting
freezegun>=1.2         # Freeze time for schedule / clock tests

# ── Linting & formatting ─────────────────────────────────────────────────────
ruff>=0.4              # Fast linter + formatter (replaces flake8 + isort)
mypy>=1.8              # Static type checking

# ── Type stubs ───────────────────────────────────────────────────────────────
types-Pillow
